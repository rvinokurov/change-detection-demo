import { Route } from '@angular/router';
import { AsyncBindingsComponent } from './async-bindings.component';

export const asyncBindingsRoutes: Route[] = [
  { path: '', component: AsyncBindingsComponent },
];
