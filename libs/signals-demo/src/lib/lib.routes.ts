import { Route } from '@angular/router';
import { SignalsDemoComponent } from './signals-demo.component';

export const signalsDemoRoutes: Route[] = [
  { path: '', component: SignalsDemoComponent },
];
